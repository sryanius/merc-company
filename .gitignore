# 이 프로젝트는 빌드 스텝도 의존성도 없다. 커밋에서 뺄 것은 사실상 없지만,
# 도구가 흘리는 것과 OS 부스러기만 막아 둔다.

# 임시 진단 스크립트 (tools/_로 시작하는 것은 일회용이다)
tools/_*.mjs

# 에디터·OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# 혹시라도 생길 경우 (지금은 쓰지 않는다)
node_modules/
*.log
