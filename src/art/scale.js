/**
 * 도트 해상도 배율 — **여기 하나가 전부의 출처다.**
 * ════════════════════════════════════════════════════════════════════════════
 *
 * ★ 왜 따로 있나
 *   `spritegen.js` 가 좌표계의 주인이지만, `parts.js` 도 파츠를 몇 배로 승격할지 알아야 한다.
 *   spritegen 이 parts 를 import 하므로 반대 방향이 생기면 **순환**이 된다.
 *   상수 하나 때문에 순환을 만들 이유가 없어서 여기로 뺐다.
 *
 * ★★ 파츠 픽셀과 조인트·포즈 숫자는 전부 **32×40 기준**으로 적혀 있다.
 *   해상도를 올릴 때 그 숫자들을 손으로 고치면 반드시 몇 개를 빠뜨린다
 *   (조인트 9개 · 포즈 오프셋 수십 개 · 방패 오프셋 · 회전축…).
 *   대신 **여기 하나로 곱한다.**
 *
 * ★ 1 로 되돌리면 옛 해상도로 즉시 돌아간다 — 비교할 때 쓴다.
 *   2 = 64×80 · 3 = 96×120.
 *
 *   3 이 지금의 천장이다: 게임은 스프라이트를 96×120 CSS px 에 그리므로
 *   그보다 크게 그리면 일반 화면에서 **축소해서 버린다** (HANDOFF §52).
 *
 * @module art/scale
 */
export const SCALE = 3;

/** 32×40 기준 논리 규격. 화면 좌표는 이쪽을 쓴다. */
export const BASE_W = 32;
export const BASE_H = 40;
export const BASE_FOOT_Y = 38;
