# 작업 규칙

## 개발 서버는 **항상 포트 5174**

```bash
node tools/serve.mjs 5174
```

- 브라우저 확인도 `http://localhost:5174` 로만 한다.
- `tools/serve.mjs` 의 기본값을 5174 로 바꿔 놨다 — 인자를 빼먹어도 5174 로 뜬다.
  (예전 기본값은 5173 이었고, 그것 때문에 «서버가 안 뜬다» 를 몇 번 겪었다.)
- 다른 포트로 띄우지 않는다. 제작자가 그 주소를 열어 두고 본다.

## 세션을 재개할 때

`docs/HANDOFF.md` 부터 읽는다. 지금까지의 결정과 **실패해서 버린 시도**가 거기 있다
(예: §65 머리 파츠 재작화는 해보고 버렸다 — 안 읽으면 또 시도하게 된다).

## 고치기 전에 잰다

이 저장소의 규칙이다. 증상을 말로 옮기지 말고 **수치로 재고**, 고친 뒤 **다시 재서** 비교한다.
검사를 새로 만들면 **버그를 심어 실제로 물리는지** 확인한다 (메타 검사).
`node tools/smoke.mjs` 가 전부 통과해야 커밋한다.

## 배포

`sw.js` 의 `CACHE` 를 매 배포마다 올린다. 새 모듈을 추가하면 `APP_SHELL` 에도 넣는다.

## Supabase 는 **다른 앱과 공유한다**

침묵의 기록자(`C:\claude\app`)가 같은 프로젝트를 쓴다 — 테이블 접두어는 `tsa_` 다.
게임 테이블은 안 건드리기로 돼 있고, 실제로 그렇다 (2026-08-26 확인).

★★ **anon 키도 공유된다.** 이 게임의 anon 키는 저장소에 공개돼 있어서
(설계상 그렇다 — RLS 가 방어선이다), **어느 쪽이든 RLS 없는 테이블을 하나 만들면
양쪽 모두에게 열린다.**

그래서 **테이블이나 정책을 만들면 반드시 돌린다:**

```bash
node tools/rlscheck.mjs
```

RLS 가 꺼졌거나 `using (true)` 로 열린 정책이 있으면 실패한다.
낯선 테이블이 생겨도 알려 준다. (판단부는 `tools/lib/rlsjudge.mjs` — 스모크가 굴려 본다.)

## SQL 함수를 만들거나 고치면 **반드시 돌린다**

```bash
node tools/sqlcheck.mjs
```

★★ plpgsql 은 문장을 **처음 실행할 때** 계획한다. 그래서 `create function` 은 멀쩡히
통과하고 **부를 때만** 터진다. **「만들어졌다」 는 아무 증거가 아니다.**

실제로 그랬다: `gold_send()` 가 `select count(*) … for update` 때문에
**내놓은 날부터 부를 때마다 죽고 있었다** (0A000). 부탁 4건이 쌓이는 동안
보내진 건 0건이었고, 아무도 몰랐다 (§109).

이 도구는 `plpgsql_check` 로 본문의 **모든 문장을 계획해 본다** — 실행하지 않고,
데이터를 한 줄도 안 건드린다. 오프라인 짝은 `tools/lib/sqllock.mjs` 이고 스모크가 굴린다.

## 업데이트할 때는 **반드시 내역을 쓴다**

`src/data/changelog.js` 에 항목을 더한다. 제작자가 정했다 (2026-09-01):

> 「업뎃할 때는 업데이트 내역 꼭 쓰고, 보안 관련된 거라 공개하면 안 되는 건
>  그냥 자잘한 버그 패치 정도로 써도 될 것 같고」

⇒ **«안 적는다» 는 선택지가 아니다.** 방어 이야기는 «자잘한 버그 패치» 로 적는다.
  사람은 «뭔가 바뀌었다» 만 알면 되고, «무엇을 어떻게 막는지» 는 알 필요가 없다.

★ 항목에 `rev` (그 소식이 나간 판번호 = `net/config.js` 의 `CLIENT_REV`)를 같이 적는다.
  화면이 맨 위 항목 옆에 찍고 **지금 내 화면의 판과 같으면 금색으로 강조**한다 —
  「고쳤다는데 내 화면이 그 판인가」 를 스크롤 없이 확인할 수 있어야 한다
  (제작자가 짚었다: 「넌 버전을 말하는데 난 몰라」).

## 업데이트 내역에 **치트 방어를 적지 않는다**

`src/data/changelog.js` 는 **플레이어 전원이 본다 — 치트 계정도 본다.**
거기에 «어떻게 걸러내는지» 나 «상한이 얼마인지» 를 적으면 그대로 우회 설명서가 된다
(사유를 숨기기로 한 §55 의 결정이 통째로 무너진다).

- 판정 경계값(전력 천장·상한 상수)을 숫자로 적지 않는다
- 「서버가 검증하지 않는다」 류로 **무엇이 안 막혔는지** 알려 주지 않는다
- 반복 시 어떻게 되는지 등 **방어가 도는 방식**을 설명하지 않는다

내부 이야기는 `docs/HANDOFF.md` 에 적는다. 스모크가 이 셋을 실제로 막는다.
